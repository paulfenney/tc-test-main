# Test for TeamCity settings in subtree

This is the main repository, which will have the TC settings as a subtree.

If this doesn't work I'll see how it plays with a submodule.

There may be another solution, like making TC create a branch for the settings
changes instead of trying to commit them directly.

First up let's see if there's a way to make TC make the changes to the branch
